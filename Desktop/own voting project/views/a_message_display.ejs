<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vote for a Candidate</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            
            color: #333;
            margin: 0;
            padding: 20px; 
            background-image: url(./images/bgkimg.avif);
            background-position: center;
            background-repeat: no-repeat; 
            background-size: 150%;
        }
        h1 {
            text-align: center;
            color: #fff;
            font-size: 2.3em;
            
        }
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-top: 40px;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 12px;
            text-align: center;
            font-size: 16px;
        }
        th {
            background-color: #4CAF50;
            color: white;
            font-size: 18px;
        }
        td img {
            display: block;
            margin: 0 auto;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        button, input[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover, input[type="submit"]:hover {
            background-color: #0056b3;
        }
        form {
            margin: 0;
        }
        tbody tr:nth-child(even) {
            background-color: antiquewhite;
        }
        tbody tr:nth-child(odd) {
            background-color: rgba(245,245,220,1);
        }
        td[colspan="4"] {
            font-size: 18px;
            color: #555;
        }
    </style>
</head>
<body>
    <h1>Messages by Users</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Message</th>
            </tr>
        </thead>
        <tbody>
            <% if (support.length > 0) { %>
                <% for (var i = 0; i < support.length; i++) { %>
                    <tr>
                        <td><%= support[i].Name %></td>
                        <td><%= support[i].Email %></td>
                        <td><%= support[i].message %></td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="4">There is no database available</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>