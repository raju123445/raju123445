<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vote for a Candidate</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            
            color: #333;
            margin: 0;
            padding: 20px; 
            background-image: url(./images/bgkimg.avif);
            background-position: center;
            background-repeat: no-repeat; 
            background-size: 150%;
        }
        h1 {
            text-align: center;
            color: #fff;
            font-size: 2.3em;
            
        }
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            margin-top: 40px;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 12px;
            text-align: center;
            font-size: 16px;
        }
        th {
            background-color: #4CAF50;
            color: white;
            font-size: 18px;
        }
        td img {
            display: block;
            margin: 0 auto;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        button, input[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover, input[type="submit"]:hover {
            background-color: #0056b3;
        }
        form {
            margin: 0;
        }
        tbody tr:nth-child(even) {
            background-color: antiquewhite;
        }
        tbody tr:nth-child(odd) {
            background-color: rgba(245,245,220,1);
        }
        td[colspan="4"] {
            font-size: 18px;
            color: #555;
        }
    </style>
</head>
<body>
    <h1>Vote for Your Candidate</h1>
    <table>
        <thead>
            <tr>
                <th>Candidate ID</th>
                <th>Symbol</th>
                <th>Candidate Name</th>
                <th>Party Name</th>
                <th>Position</th>
                <th>Vote Button</th>
            </tr>
        </thead>
        <tbody>
            <% if (candidate.length > 0) { %>
                <% for (var i = 0; i < candidate.length; i++) { %>
                    <tr>
                        <td><%= candidate[i].candidate_id %></td>
                        <td>
                            <img src="/uploads/<%= candidate[i].symbol_path %>" alt="Symbol" style="width:50px; height:50px;">
                        </td>
                        <!-- <td><%= candidate[i].nam̧̧ȩ , candidate[i].name_in_kannada%></td> -->
                        <!-- <td><%= candidate[i].name + "<br>" + candidate[i].name_in_kannada %></td> -->
                        <td>
                            <%= candidate[i].name_in_kannada %><br>
                            <span style="font-size: 16px; "><%= candidate[i].name %></span>
                        </td>
                        
                        <!-- <td><%= candidate[i].party %></td>party_in_kannada -->
                        <td>
                            <%= candidate[i].party_in_kannada %><br>
                            <span style="font-size: 16px; "><%= candidate[i].party %></span>
                        </td>
                        <td><%= candidate[i].position %></td>
                        <td><form action="/voted" method="post">
                            <input type="submit" name="candidate_id" value="<%= candidate[i].candidate_id %>">
                        </form></td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="4">There is no database available</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>


<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vote for a Candidate</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: center;
        }
        th {
            background-color: #f4f4f4;
        }
        button {
            padding: 5px 10px;
            background-color: #28a745;
            color: white;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Vote for Your Candidate</h1>
    <table>
        <thead>
            <tr>
                <th>Candidate ID</th>
                <th>Symbol</th>
                <th>Candidate Name</th>
                <th>Party Name</th>
                <th>Position</th>
                <th>Vote Button</th>
            </tr>
        </thead>
        <tbody>
            <% if (candidate.length > 0) { %>
                <% for (var i = 0; i < candidate.length; i++) { %>
                    <tr>
                        <td><%= candidate[i].candidate_id %></td>
                        <td>
                            <img src="/uploads/<%= candidate[i].symbol_path %>" alt="Symbol" style="width:50px; height:50px;">
                        </td>
                        <td><%= candidate[i].name %></td>
                        <td><%= candidate[i].party %></td>
                        <td><%= candidate[i].position %></td>
                        <td><form action="/voted" method="post">
                            <input type="submit" name="candidate_id" value="<%= candidate[i].candidate_id %>">
                        </form></td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="4">There is no database available</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html> -->
